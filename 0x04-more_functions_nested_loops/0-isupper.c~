#include "main.h"

/**
 * _isupper - is upper
 * Return: sucess or fail
 * @c: letter to check
 */


int _isupper(int c)
{
	char C;

	C = c;
	if (C >= 65 || C <= 90)
		return (1);
	else
		return (0);
}
