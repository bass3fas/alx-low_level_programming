#include "main.h"
/**
 * wildcmp - something
 * @s1: str
 * @s2: str
 * Return: int
 */
int wildcmp(char *s1, char *s2)
{
	if (*s1 == '\0' && *s2 == '\0')
		return (1);
	if (*s2 == '*' && *s2 + 1 != '\0' && *s1 == '\0')
		return (0);
	
