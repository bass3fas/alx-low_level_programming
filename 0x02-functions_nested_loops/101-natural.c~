#include "main.h"
#include <stdio.h>
/**
 * natural - prints divisible by 3 and 5
 */



void natural(void)
{
	int i;

	for (i = 0; i < 1024; i++)
	{
		if (i % 3 == 0 || i % 5 == 0)
			printf("%d\n", i);
	}
}
