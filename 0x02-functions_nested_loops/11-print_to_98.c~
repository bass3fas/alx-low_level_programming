#include "main.h"
#include <stdio.h>
/**
 * print_to_98 - print to 98
 * @n: number to start
 */


void print_to_98(int n)
{
	int i, k;

	if (n < 98)
	{
		for (i = n; i <= 98; i++)
			printf("%d, ", i);
	}
	else
	{
		for (k = n; k >= 98; k--)
			printf("%d, ", k);
	}
}
