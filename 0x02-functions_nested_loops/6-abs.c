#include "main.h"

/**
 * _abs - calculates absolute value of a number
 * @n: number to be tested
 * Return: the absolute value
 */




int _abs(int n)
	{
		if (n < 0)
			return (-n);
		else
			return (n);
	}
