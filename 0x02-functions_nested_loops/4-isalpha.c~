#include "main.h"

/**
  * _isalpha - detects if character is letter
  * @c: the letter to be tested
  * Return: 1 if true, 0 if false
  */


int _isalpha(int c)
	{
		if (c >= 65 && c <= 90)
		{
			if (c >= 97 && c <= 122)
				return (1);
		}
		else
			return (0);
	}
