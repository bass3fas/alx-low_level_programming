#include "main.h"


/**
 * puts-half - puts
 * @str: n
 */

void puts_half(char *str)
{
	int c, l;

	for (c = 0; str[c] != '\0'; c++)
		;
	if (c % 2 == 0)
		l = c / 2;
	else
		l = (c - 1) / 2;

	for (c = 0; c < l; c++)
		_putchar(str[c]);

	_putchar('\n');
}
