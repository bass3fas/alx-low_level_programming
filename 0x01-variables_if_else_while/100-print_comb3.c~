#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints possible combination of 2 digits
 * Return: Always (0)
 */

int main(void)
{
	int i;
	int j;

	i = 48;


	while (i <= 56)
	{
		j = 49;

		while (j <= 57)
		{
			putchar(i);
			putchar(j);
			if (i != 56 || j != 57)
			{
				putchar(',');
				putchar(' ');
			}
			j++;
		}
		i++;
	}
	putchar('\n');
	return (0);
}
