#include "lists.h"
/**
 * print_listint - something
 * @h: var
 * Return: size
 */
size_t print_listint(const listint_t *h)
{
	size_t count = 0;

	while (h)
	{
		printf("%d\n", h->n);
		count++;
	}
	return (count);
}
